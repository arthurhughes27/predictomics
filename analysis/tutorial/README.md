# predictomics tutorial

This folder contains a self-contained demonstration of `predictomics`'s two
main entry points, `predict_cv()` and `compare_pipelines()`, on simulated
data - intended as the source material for a package tutorial section in the
thesis, in the same style as the `SurrogateRank` tutorial (chapter 3, Section
"The `SurrogateRank` R package").

## Running it

From the package root (with `predictomics` installed):

```r
source("analysis/tutorial/tutorial.R")
```

This writes two kinds of output, neither of which is committed to the repo
(they're regenerated by the script, and depend on the local R/package
versions):

- `figures/*.pdf` - one PDF per plot produced during the tutorial, ready for
  `\includegraphics{}`.
- `outputs/*.txt` - one plain-text file per console output (`str()`,
  `print()`, table previews), ready to paste into a
  `\begin{minted}{text} ... \end{minted}` block, or to bring in directly via
  `\verbatiminput{}`/`\lstinputlisting{}` if you'd rather not paste by hand.

## Mapping outputs onto the chapter

The script is split into two parts, each producing the same three kinds of
material the `SurrogateRank` tutorial uses - narrative code, a console-text
table/summary, and a figure:

| # | What | File | Suggested LaTeX use |
|---|------|------|----------------------|
| 1.1 | `str(sim)` | `outputs/01_simulate_str.txt` | `\begin{minted}{text}` block, as in the `SurrogateRank` `str(full_data)` example |
| 1.2 | `predict_cv()` call itself | (paste the R code block directly) | `\begin{minted}{r}` block |
| 1.3 | `print(fit)` | `outputs/02_predict_cv_print.txt` | `\begin{minted}{text}` block, or reformat the key lines (RMSE/sRMSE/R2/SpearmanR) into a table like Table `tab:tutorial-evaluate` |
| 1.3 | Observed-vs-predicted scatter | `figures/tutorial_predict_cv_scatter.pdf` | `\includegraphics`, analogous to Figure `fig:tutorial-evaluate` |
| 1.3 | Embedded selection stability (lasso) | `figures/tutorial_selection_stability.pdf` | `\includegraphics`, analogous to Figure `fig:tutorial-screen` |
| 1.3 | Feature importance | `figures/tutorial_feature_importance.pdf` | `\includegraphics` |
| 1.3 | Ground-truth signal genesets | `outputs/03_signal_genesets.txt` | inline text, or a footnote confirming the pipeline recovers the simulated signal |
| 2.1 | `compare_pipelines()` call itself | (paste the R code block directly) | `\begin{minted}{r}` block |
| 2.2 | `print(cmp)` | `outputs/04_compare_pipelines_print.txt` | `\begin{minted}{text}` block |
| 2.2 | Full results table | `outputs/05_compare_pipelines_results.txt` | source for a hand-built `\begin{table}` (as in Table `tab:tutorial-screen`), or `\begin{minted}{text}` directly |
| 2.2 | Comparison bar chart | `figures/tutorial_compare_pipelines.pdf` | `\includegraphics` |

## Editing/extending

`tutorial.R` is deliberately linear and narrated with section comments so
that blocks of it can be copied into `\begin{minted}{r}` environments with
minimal editing - the code as written is what should appear in the chapter,
not a wrapper around it. If you want to demonstrate a different pipeline
axis (e.g. `option_type = "engineering"` or `"selection"` instead of
`"model"` in Part 2, or a paired/`gene_level_fc` design via
`simulate_predictomics_data(design = "paired")`), duplicate Part 2's
structure with the new `option_choices` rather than editing it in place, so
the existing figures/outputs stay reproducible from the committed script.
