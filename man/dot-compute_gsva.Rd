% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_engineering.R
\name{.compute_gsva}
\alias{.compute_gsva}
\title{Compute Gene Set Variation Analysis (GSVA) scores}
\usage{
.compute_gsva(X, genesets, kcdf, tau, max_diff, abs_ranking, min_size, max_size)
}
\arguments{
\item{X}{Numeric matrix of dimensions n (samples) x p (features), post
column-wise transformation.}

\item{genesets}{Named list of character vectors of feature names.}

\item{kcdf}{Character string. Kernel used to estimate the cumulative
distribution function for each gene. One of "Gaussian", "Poisson", or
"none".}

\item{tau}{Numeric. Exponent weight for the random walk statistic.}

\item{max_diff}{Logical. If \code{TRUE}, the GSVA score is the difference
between the largest positive and negative random walk deviations; if
\code{FALSE}, the largest absolute deviation is used.}

\item{abs_ranking}{Logical. Whether to use absolute gene expression
ranking (ignoring the sign of gene-level changes).}

\item{min_size}{Positive integer. Minimum geneset size (after intersecting
with \code{colnames(X)}) required for a geneset to be scored.}

\item{max_size}{Numeric. Maximum geneset size allowed.}
}
\value{
Numeric matrix of GSVA scores (samples x genesets), in the same
geneset order as \code{genesets}.
}
\description{
Internal workhorse for \code{agg_method = "gsva"}, called by
\code{\link{.aggregate_genesets}}. Computes GSVA scores via
\code{GSVA::gsva()}/\code{GSVA::gsvaParam()}. As with ssGSEA, GSVA scores
are computed independently on whatever matrix is supplied (there are no
training-derived parameters that can be stored and reapplied, since GSVA
estimates a per-gene distribution across the samples supplied in a single
call).
}
\keyword{internal}
