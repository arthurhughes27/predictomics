% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_fold.R
\name{.run_fold}
\alias{.run_fold}
\title{Execute a single CV fold}
\usage{
.run_fold(
  k,
  fold_ids,
  X_processed,
  Y,
  outside_cv,
  engineering_params,
  selection_params,
  model_params,
  treatment,
  treatment_mat,
  covariate_mat,
  is_paired_rise = FALSE,
  X_full = NULL,
  Y_full = NULL,
  treatment_full = NULL,
  covariate_mat_full = NULL
)
}
\arguments{
\item{k}{Integer. Fold index.}

\item{fold_ids}{Integer vector of fold assignments.}

\item{X_processed}{Numeric matrix. Predictor matrix after any outside-CV
steps.}

\item{Y}{Numeric vector. Full response vector.}

\item{outside_cv}{Logical. Whether outside-CV steps have already been
applied.}

\item{engineering_params, selection_params, model_params}{Pipeline parameter
lists.}

\item{treatment}{Original treatment vector or \code{NULL}.}

\item{treatment_mat}{Numeric matrix of treatment columns or \code{NULL}.}

\item{covariate_mat}{Numeric matrix of covariate columns or \code{NULL}.}
}
\value{
A named list with:
\describe{
\item{\code{predictions}}{Numeric vector of length n with predictions
for test samples in their original positions and zeros elsewhere.}
\item{\code{selection_diagnostics}}{Named list of explicit selection
results for this fold, or \code{NULL}.}
\item{\code{embedded_selection_diagnostics}}{Named list of embedded
selection results (lasso/glmnet non-zero features) for this fold,
or \code{NULL}.}
}
}
\description{
Internal workhorse called by \code{future.apply::future_lapply} in
\code{\link{predict_cv}}. Runs the full pipeline for one fold: engineering,
selection, protected predictor appending, model fitting, and prediction.
Returns predictions for the test partition, optional explicit selection
diagnostics, and optional embedded selection diagnostics.
}
\keyword{internal}
