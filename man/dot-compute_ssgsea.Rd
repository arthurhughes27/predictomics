% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_engineering.R
\name{.compute_ssgsea}
\alias{.compute_ssgsea}
\title{Compute single-sample GSEA (ssGSEA) enrichment scores}
\usage{
.compute_ssgsea(X, genesets, alpha, min_size, max_size, normalize)
}
\arguments{
\item{X}{Numeric matrix of dimensions n (samples) x p (features), post
column-wise transformation.}

\item{genesets}{Named list of character vectors of feature names.}

\item{alpha}{Numeric. Exponent weight for the running-sum statistic.}

\item{min_size}{Positive integer. Minimum geneset size (after intersecting
with \code{colnames(X)}) required for a geneset to be scored.}

\item{max_size}{Numeric. Maximum geneset size allowed.}

\item{normalize}{Logical. Whether to apply the Barbie et al. (2009)
normalisation of ssGSEA scores.}
}
\value{
Numeric matrix of ssGSEA scores (samples x genesets), in the same
geneset order as \code{genesets}.
}
\description{
Internal workhorse for \code{agg_method = "ssgsea"}, called by
\code{\link{.aggregate_genesets}}. Computes ssGSEA scores via
\code{GSVA::gsva()}/\code{GSVA::ssgseaParam()}. Unlike the other
aggregation methods, ssGSEA scores are computed independently on whatever
matrix is supplied (there are no training-derived parameters that can be
stored and reapplied, since the enrichment statistic is computed per
sample from that sample's own gene ranking).
}
\keyword{internal}
