% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_model.R
\name{.extract_glmnet_features}
\alias{.extract_glmnet_features}
\title{Extract non-zero coefficient features from a fitted glmnet model}
\usage{
.extract_glmnet_features(
  caret_fit,
  method,
  user_method,
  fixed_alpha,
  col_names
)
}
\arguments{
\item{caret_fit}{A fitted caret train object.}

\item{method}{Character. The resolved method (\code{"glmnet"} or \code{"lm"}).}

\item{user_method}{Character. The user-supplied method name.}

\item{fixed_alpha}{Numeric or \code{NULL}. The fixed alpha value for lasso
or ridge, or \code{NULL} for glmnet (alpha tuned).}

\item{col_names}{Character vector. Sanitised column names from training.}
}
\value{
Character vector of selected feature names (in original
pre-sanitisation names where possible), or \code{NULL}.
}
\description{
For \code{"lasso"} and \code{"glmnet"} fits where alpha > 0, extracts the
names of features with non-zero coefficients at the selected lambda from the
final model. Returns \code{NULL} for \code{"ridge"} (alpha = 0), \code{"lm"},
and \code{"ranger"}, since these methods do not produce sparse solutions.
}
\keyword{internal}
