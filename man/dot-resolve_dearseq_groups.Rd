% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_selection.R
\name{.resolve_dearseq_groups}
\alias{.resolve_dearseq_groups}
\title{Resolve the grouping variable, sample_group, and row subset for dearseq}
\usage{
.resolve_dearseq_groups(X_train, treatment, individual_id, timepoint, dearseq_mode)
}
\arguments{
\item{X_train}{Numeric matrix. Training predictor matrix.}

\item{treatment}{Binary numeric vector (0/1), factor, or \code{NULL}.}

\item{individual_id}{Vector identifying individuals, or \code{NULL}.}

\item{timepoint}{Binary numeric vector (0/1), or \code{NULL}.}

\item{dearseq_mode}{Character string. One of \code{"classic"} or
\code{"paired"}.}
}
\value{
A named list with \code{keep} (integer row indices), \code{group_var}
(numeric grouping vector), \code{group_name} (character), and
\code{sample_group} (vector or \code{NULL}).
}
\description{
Shared row-restriction logic for \code{\link{.compute_dearseq_scores}} and
\code{\link{.compute_dearseq_geneset_scores}}. For \code{"classic"} mode,
restricts to \code{timepoint == 1} rows when \code{individual_id} is
supplied and returns \code{treatment} as the grouping variable. For
\code{"paired"} mode, restricts to \code{treatment == 1} rows when
\code{treatment} is supplied and returns \code{timepoint} as the grouping
variable with \code{individual_id} as \code{sample_group}.
}
\keyword{internal}
