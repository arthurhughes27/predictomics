% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_selection.R, R/validate_inputs.R
\name{.validate_selection_params}
\alias{.validate_selection_params}
\title{Validate selection_params}
\usage{
.validate_selection_params(params, p)

.validate_selection_params(params, p)
}
\arguments{
\item{params}{The \code{selection_params} list.}

\item{p}{Integer. Number of features in \code{X_train}, used to bound
\code{top_n}.}
}
\value{
Invisibly returns \code{NULL} if validation passes.

Invisibly returns \code{NULL} if validation passes.
}
\description{
Checks that \code{selection_params} specifies a supported method, that
exactly one of \code{top_n} or \code{threshold} is specified (or both,
since \code{top_n} takes precedence), and that their values are valid
relative to the number of available features \code{p}.

Checks that \code{selection_params} specifies a supported method, that
exactly one of \code{top_n} or \code{threshold} is specified (or both,
since \code{top_n} takes precedence), and that their values are valid
relative to the number of available features \code{p}.
}
\keyword{internal}
