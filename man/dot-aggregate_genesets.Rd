% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_engineering.R
\name{.aggregate_genesets}
\alias{.aggregate_genesets}
\title{Aggregate features into genesets}
\usage{
.aggregate_genesets(
  X,
  genesets,
  agg_method,
  feature_names,
  is_train,
  pc1_loadings,
  col_transform,
  ssgsea_alpha = 0.25,
  ssgsea_min_size = 1L,
  ssgsea_max_size = Inf,
  ssgsea_normalize = TRUE,
  gsva_kcdf = "Gaussian",
  gsva_tau = 1,
  gsva_max_diff = TRUE,
  gsva_abs_ranking = FALSE,
  gsva_min_size = 1L,
  gsva_max_size = Inf
)
}
\arguments{
\item{X}{Numeric matrix post column-wise transformation.}

\item{genesets}{Named list of character vectors of feature names.}

\item{agg_method}{Character string. One of "mean", "median", "sum", "max",
"pc1", "ssgsea", "gsva".}

\item{feature_names}{Character vector of column names of \code{X}.}

\item{is_train}{Logical. If \code{TRUE}, PC1 loadings are fitted from
\code{X}. If \code{FALSE}, \code{pc1_loadings} must be supplied. Ignored
for \code{agg_method \%in\% c("ssgsea", "gsva")}, which are recomputed
from \code{X} regardless.}

\item{pc1_loadings}{Named list of PC1 loading vectors (one per geneset),
or \code{NULL} when \code{is_train = TRUE}.}

\item{ssgsea_alpha, ssgsea_min_size, ssgsea_max_size, ssgsea_normalize}{ssGSEA tuning parameters, only used when \code{agg_method = "ssgsea"}.
See \code{\link{run_engineering}}.}

\item{gsva_kcdf, gsva_tau, gsva_max_diff, gsva_abs_ranking, gsva_min_size, gsva_max_size}{GSVA tuning parameters, only used when \code{agg_method = "gsva"}. See
\code{\link{run_engineering}}.}
}
\value{
Numeric matrix of aggregated features (samples x genesets), with
PC1 loadings attached as an attribute when \code{is_train = TRUE} and
\code{agg_method = "pc1"}.
}
\description{
Internal workhorse for geneset aggregation. Called by both
\code{run_engineering} (training) and \code{predict_engineering} (test).
When \code{is_train = TRUE} and \code{agg_method = "pc1"}, PC1 loadings
are computed and attached as an attribute of the returned matrix for
retrieval by \code{run_engineering}.
}
\keyword{internal}
