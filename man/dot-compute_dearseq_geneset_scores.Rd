% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_selection.R
\name{.compute_dearseq_geneset_scores}
\alias{.compute_dearseq_geneset_scores}
\title{Compute dearseq geneset-level adjusted p-value scores for feature selection}
\usage{
.compute_dearseq_geneset_scores(
  X_train,
  covariates,
  treatment,
  individual_id,
  timepoint,
  dearseq_mode,
  genesets,
  which_test,
  preprocessed,
  padjust_methods,
  which_weights,
  n_perm,
  bw,
  kernel
)
}
\arguments{
\item{X_train}{Numeric matrix. Training predictor matrix (samples x
features).}

\item{covariates}{Numeric matrix or data frame, or \code{NULL}. Passed to
\code{dgsa_seq()} as a full design matrix (with intercept) via
\code{\link{.build_dearseq_covariates}}.}

\item{treatment}{Binary numeric vector (0/1), factor, or \code{NULL}.}

\item{individual_id}{Vector identifying individuals, or \code{NULL}.}

\item{timepoint}{Binary numeric vector (0/1), or \code{NULL}.}

\item{dearseq_mode}{Character string. One of \code{"classic"} or
\code{"paired"}.}

\item{genesets}{Named list of character vectors of feature names.}

\item{which_test, preprocessed, padjust_methods, which_weights, n_perm, bw, kernel}{Arguments passed directly to \code{dearseq::dgsa_seq()}.}
}
\value{
Named numeric vector of adjusted p-values, one per geneset, in
\code{names(genesets)} order (restricted to genesets with at least one
feature present in \code{X_train}).
}
\description{
Calls \code{dearseq::dgsa_seq()} to test genesets (rather than individual
genes) for differential expression across a binary grouping variable, in
the same \code{"classic"}/\code{"paired"} row-restriction modes as
\code{\link{.compute_dearseq_scores}}. Returns adjusted p-values indexed
by geneset name (not gene name); \code{\link{run_selection}} resolves
selected genesets back to their member gene names.
}
\keyword{internal}
